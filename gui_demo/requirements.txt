# 猫脸识别 GUI 的 Python 依赖。
#
# 注意：
# - tkinter 是 Python 标准库，不在此列；Linux 需额外安装系统包 python3-tk
# - OpenCV 在 Linux 上还需系统库 libgl1、libglib2.0-0（见 README.md）
# - 如需 CUDA 加速，请先用 PyTorch 官方向导安装对应 CUDA 版本的
#   torch / torchvision，再安装本文件其余依赖（见 README.md）
#
numpy>=2.0,<3
opencv-python>=4.11,<5
pillow>=11,<13
timm>=1.0,<2
torch>=2.8,<3
torchvision>=0.23,<1
ultralytics>=8.4,<9
