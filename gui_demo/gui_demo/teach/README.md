# 猫脸识别流水线 · 算法详解

浏览器打开 `猫脸识别流水线详解.html` 即可阅读（图片在 `images/` 下，使用相对路径引用，整个文件夹可整体拷贝）。

- `猫脸识别流水线详解.html` — 主文档：从输入帧到身份输出的 7 个算法环节逐模块详解，含真实运行数据、代码引用、静态示意图与图表（纯 HTML + CSS + SVG，无脚本依赖，离线可读）
- `images/` — 演示图片：真实测试照片、真实检测标注结果、脸/身 ROI 裁剪、注册照集、多猫摄像头快照等

所有图片与数据来自真实运行 `gui_demo/run_gui.py` 完整链路（CUDA / YOLO11n / MobileOne-S1 384D）的结果；示意图为算法机制图示。
